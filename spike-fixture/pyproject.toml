[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "spike-fixture"
version = "0.1.0"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.pytest-gremlins]
paths = ["mathlib.py"]
